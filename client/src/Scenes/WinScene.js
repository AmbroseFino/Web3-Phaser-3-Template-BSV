import 'phaser';
import config from '../Config/config';

export default class WinScene extends Phaser.Scene {
  constructor () {
    super('Win');
    }

    init (data) {

    }
    create() {
     
    }
    update () {
  
    } 

};
